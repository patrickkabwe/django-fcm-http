from setuptools import setup

setup(
    keywords=['django', 'fcm', 'django-fcm-http', 'django-fcm', 'push',
              'notification', 'http', 'rest', 'push notification', 'django fcm', 'django-fcm'],
    install_requires=[
        'django>=3.0',
        'oauth2client==4.1.3',
        'requests>=2.28.1'
    ],
)
